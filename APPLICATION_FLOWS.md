# 🔄 Fluxos da Aplicação

Este documento mostra visualmente como funcionam os principais fluxos do URL Shortener Backend.

## 1️⃣ Fluxo de Autenticação

```
┌─────────────────┐
│   Novo Usuário  │
└────────┬────────┘
         │
         ▼
┌──────────────────────────────────┐
│ POST /api/auth/register          │
│ { email, password, name }        │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Validar dados                    │
│ - Email válido?                  │
│ - Email já existe?               │
│ - Senha forte?                   │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ OK     │ ❌ Erro
    ▼           ▼
 Hash com   Retorna
 bcrypt     erro 400
    │
    ▼
┌──────────────────────────────────┐
│ Criar usuário no banco           │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Retornar 201 Created             │
│ { id, email, name, ... }         │
└──────────────────────────────────┘
         
         
         ┌──────────────────────────────────┐
         │   Usuário Existente              │
         └────────┬─────────────────────────┘
                  │
                  ▼
         ┌──────────────────────────────────┐
         │ POST /api/auth/login             │
         │ { email, password }              │
         └────────┬─────────────────────────┘
                  │
                  ▼
         ┌──────────────────────────────────┐
         │ Buscar usuário por email         │
         └────────┬─────────────────────────┘
                  │
             ┌────┴────────┐
             │ ✅ Encontrado
             │ ❌ Não existe
             ▼             ▼
          Comparar      Retorna
          senha com     erro 401
          bcrypt        
             │
             ├─ ✅ Correto
             └─ ❌ Incorreto
                │         │
                ▼         ▼
             Gerar    Retorna
             JWT      erro 401
             (1 dia)
                │
                ▼
         ┌──────────────────────────────────┐
         │ Retornar 200 OK                  │
         │ { token: "eyJhbGc..." }          │
         └──────────────────────────────────┘
```

---

## 2️⃣ Fluxo de Criar URL Encurtada

```
┌──────────────────────────────────┐
│ POST /api/urls                   │
│ Headers: Authorization: Bearer   │
│ Body: { originalUrl, ... }       │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Validar JWT Token                │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Válido  │ ❌ Inválido
    ▼           ▼
 Continuar  Retorna
 com userId erro 401
    │
    ▼
┌──────────────────────────────────┐
│ Validar URL original             │
│ - URL format válido?             │
│ - URL não vazia?                 │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ OK     │ ❌ Erro
    ▼           ▼
 Continuar  Retorna
            erro 400
    │
    ▼
┌──────────────────────────────────┐
│ Definir short code               │
├──────────────────────────────────┤
│ Se customCode foi informado:     │
│ - Valida tamanho (3-20)          │
│ - Verifica se já existe          │
│ Senão:                           │
│ - Gera código aleatório (nanoid) │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ OK     │ ❌ Erro
    ▼           ▼
 Continuar  Retorna
            erro 400
    │
    ▼
┌──────────────────────────────────┐
│ Criar ShortenedUrl no banco      │
│ - userId                         │
│ - originalUrl                    │
│ - shortCode (unique)             │
│ - title (opcional)               │
│ - description (opcional)         │
│ - expiresAt (opcional)           │
│ - clicks = 0                     │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Retornar 201 Created             │
│ {                                │
│   id, originalUrl, shortCode,    │
│   shortUrl, clicks, title, ...   │
│ }                                │
└──────────────────────────────────┘
```

---

## 3️⃣ Fluxo de Redirecionar (Público)

```
┌──────────────────────────────────┐
│ GET /:shortCode                  │
│ (Sem autenticação)               │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Buscar ShortenedUrl por code     │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Existe  │ ❌ Não existe
    ▼           ▼
 Continuar  Retorna
            erro 404
    │
    ▼
┌──────────────────────────────────┐
│ Verificar expiração              │
│ - expiresAt é null?              │
│ - now <= expiresAt?              │
└────────┬─────────────────────────┘
         │
    ┌────┴──────────┐
    │ ✅ Válido     │ ❌ Expirado
    ▼              ▼
 Continuar     Retorna
               erro 410
    │
    ▼
┌──────────────────────────────────┐
│ Registrar Analytics              │
│ - user_agent do header           │
│ - referer                        │
│ - ip_address                     │
│ - timestamp atual                │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Incrementar contador              │
│ clicks = clicks + 1              │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Redirecionar                     │
│ 301 Moved Permanently            │
│ Location: originalUrl            │
└──────────────────────────────────┘
```

---

## 4️⃣ Fluxo de Listar URLs

```
┌──────────────────────────────────┐
│ GET /api/urls?page=1&limit=10    │
│ Headers: Authorization: Bearer   │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Validar JWT Token                │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Válido  │ ❌ Inválido
    ▼           ▼
 Continuar  Retorna
 com userId erro 401
    │
    ▼
┌──────────────────────────────────┐
│ Preparar query                   │
│ - filter: userId = req.userId    │
│ - skip: (page-1) * limit         │
│ - take: limit                    │
│ - orderBy: { [sortBy]: order }   │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Buscar URLs no banco             │
│ SELECT * FROM shortened_urls     │
│ WHERE user_id = ?                │
│ ORDER BY ? LIMIT ? OFFSET ?      │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Contar total de URLs do usuário  │
│ SELECT COUNT(*) FROM ...         │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Formatar resposta                │
│ - urls com shortUrl gerada       │
│ - pagination info                │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Retornar 200 OK                  │
│ {                                │
│   urls: [ { ... }, ... ],        │
│   pagination: {                  │
│     page, limit, total, pages    │
│   }                              │
│ }                                │
└──────────────────────────────────┘
```

---

## 5️⃣ Fluxo de Atualizar URL

```
┌──────────────────────────────────┐
│ PUT /api/urls/urls/:id           │
│ Headers: Authorization: Bearer   │
│ Body: { title, description, ... }│
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Validar JWT Token                │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Válido  │ ❌ Inválido
    ▼           ▼
 Continuar  Retorna
 com userId erro 401
    │
    ▼
┌──────────────────────────────────┐
│ Buscar URL no banco              │
│ WHERE id = ? AND user_id = ?     │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Existe  │ ❌ Não existe
    ▼           ▼
 Continuar  Retorna
            erro 404
    │
    ▼
┌──────────────────────────────────┐
│ Verificar ownership              │
│ url.userId === req.userId?       │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Sim     │ ❌ Não
    ▼           ▼
 Continuar  Retorna
            erro 403
    │
    ▼
┌──────────────────────────────────┐
│ Atualizar campos                 │
│ - title (se fornecido)           │
│ - description (se fornecido)     │
│ - expiresAt (se fornecido)       │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Persistir no banco               │
│ UPDATE shortened_urls            │
│ WHERE id = ?                     │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Retornar 200 OK                  │
│ { id, originalUrl, shortCode,    │
│   title, description, updated,   │
│   ... }                          │
└──────────────────────────────────┘
```

---

## 6️⃣ Fluxo de Analytics

```
┌──────────────────────────────────┐
│ GET /api/urls/:id/analytics      │
│ Headers: Authorization: Bearer   │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Validar JWT Token                │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Válido  │ ❌ Inválido
    ▼           ▼
 Continuar  Retorna
            erro 401
    │
    ▼
┌──────────────────────────────────┐
│ Buscar URL                       │
│ WHERE id = ?                     │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Existe  │ ❌ Não existe
    ▼           ▼
 Continuar  Retorna
            erro 404
    │
    ▼
┌──────────────────────────────────┐
│ Verificar ownership              │
│ url.userId === req.userId?       │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Sim     │ ❌ Não
    ▼           ▼
 Continuar  Retorna
            erro 403
    │
    ▼
┌──────────────────────────────────┐
│ Buscar analytics                 │
│ FROM url_analytics               │
│ WHERE url_id = ?                 │
│ ORDER BY clicked_at DESC         │
│ LIMIT ? OFFSET ?                 │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Contar total                     │
│ SELECT COUNT(*) FROM ...         │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Retornar 200 OK                  │
│ {                                │
│   analytics: [                   │
│     {id, userAgent, referer,     │
│      ipAddress, country,         │
│      clickedAt}                  │
│   ],                             │
│   pagination: { ... }            │
│ }                                │
└──────────────────────────────────┘
```

---

## 7️⃣ Fluxo de Deletar URL

```
┌──────────────────────────────────┐
│ DELETE /api/urls/urls/:id        │
│ Headers: Authorization: Bearer   │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Validar JWT Token                │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Válido  │ ❌ Inválido
    ▼           ▼
 Continuar  Retorna
            erro 401
    │
    ▼
┌──────────────────────────────────┐
│ Buscar URL                       │
│ WHERE id = ?                     │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Existe  │ ❌ Não existe
    ▼           ▼
 Continuar  Retorna
            erro 404
    │
    ▼
┌──────────────────────────────────┐
│ Verificar ownership              │
│ url.userId === req.userId?       │
└────────┬─────────────────────────┘
         │
    ┌────┴──────┐
    │ ✅ Sim     │ ❌ Não
    ▼           ▼
 Continuar  Retorna
            erro 403
    │
    ▼
┌──────────────────────────────────┐
│ Deletar URL                      │
│ DELETE FROM shortened_urls       │
│ WHERE id = ?                     │
│                                  │
│ (Cascata deleta analytics!)      │
│ DELETE FROM url_analytics        │
│ WHERE url_id = ?                 │
└────────┬─────────────────────────┘
         │
         ▼
┌──────────────────────────────────┐
│ Retornar 204 No Content          │
└──────────────────────────────────┘
```

---

## 📊 Resumo de Status

| Ação | Sucesso | Erro |
|------|---------|------|
| Criar URL | 201 | 400, 401 |
| Listar URLs | 200 | 401 |
| Obter detalhes | 200 | 401, 403, 404 |
| Atualizar | 200 | 401, 403, 404 |
| Deletar | 204 | 401, 403, 404 |
| Redirecionar | 301 | 404, 410 |
| Analytics | 200 | 401, 403, 404 |

---

## 🔄 Sequência de Uso Típico

```
1. REGISTRAR NOVO USUÁRIO
   POST /api/auth/register
   
2. FAZER LOGIN
   POST /api/auth/login
   ← Recebe TOKEN
   
3. CRIAR URL ENCURTADA
   POST /api/urls
   + Authorization: Bearer TOKEN
   
4. LISTAR URLS CRIADAS
   GET /api/urls
   + Authorization: Bearer TOKEN
   
5. COMPARTILHAR SHORT URL
   Compartilha: http://localhost:3000/abc123
   
6. USUÁRIO CLICA NO LINK
   GET /abc123
   ← Redireciona para URL original
   ← Analytics registrado automaticamente
   
7. VERIFICAR ANALYTICS
   GET /api/urls/:id/analytics
   + Authorization: Bearer TOKEN
   ← Ver quem clicou
   
8. (OPCIONAL) ATUALIZAR
   PUT /api/urls/:id
   + Authorization: Bearer TOKEN
   
9. (OPCIONAL) DELETAR
   DELETE /api/urls/:id
   + Authorization: Bearer TOKEN
```

---

## 💾 Estado de Dados

```
Request Flow:
┌──────────┐
│ Cliente  │
└────┬─────┘
     │ HTTP Request
     ▼
┌──────────────┐
│ Express.js   │
│ - Parse JSON │
│ - Validar    │
└────┬─────────┘
     │ Data processada
     ▼
┌──────────────┐
│ Controller   │
│ - Lógica     │
│ - Validação  │
└────┬─────────┘
     │ Query Prisma
     ▼
┌──────────────┐
│ Prisma ORM   │
│ - Build SQL  │
└────┬─────────┘
     │ SQL Query
     ▼
┌──────────────┐
│ PostgreSQL   │
│ - Executar   │
│ - Retornar   │
└────┬─────────┘
     │ Dados
     ▼
┌──────────────┐
│ Express.js   │
│ - Serializar │
│ - JSON       │
└────┬─────────┘
     │ HTTP Response
     ▼
┌──────────┐
│ Cliente  │
└──────────┘
```

---

**Última Atualização**: 2024
**Versão**: 1.0.0
**Status**: ✅ Documentado
